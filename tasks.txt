контекст:

один файл для беку та папочка для фронту
створити бд треба з файлику .sqlite, там є пару записів в кожній таблиці. усього 3 таблиці - alerts, projects, alert_events
у мене є 2 схожі ендпоінти, які повертають просто список усіх алертів. але одне чисто запитом з скл, а друге з sqlalchemy


що треба зробити:

щоб запустити бек - треба виправити баг. я імпортую енв змінну PORT з енв файлу який не створено і не вказано про це в рідмі :white_check_mark: 
додати можливість фільтрувати алерт. перше завдання - по полю яке є в таблиці alert - severity. якщо це вийшло - треба заджойнити до алертів проєкти (їх назву) і додати фільтрацію по назві проєкту
3. запустити фронтенд. там зараз замокані дані, і там є логічна помилка, треба спочатку це виправити
5. додати джойн даних з третьої таблиці alert_event, там треба агрегація - кількість записів з таблиці по конкретному алерту
4. підключити ендпоінт який повертає алерти (зараз там замокані значення)

якщо буде час, то далі:

підключити фільтрацію з бекенду - передавати на ендпоінт відповідні параметри (зараз на фронтенді фільтрація)